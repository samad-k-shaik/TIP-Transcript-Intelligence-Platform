version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/datasets:/app/datasets
      - ./backend/parquet:/app/parquet
      - ./backend/logs:/app/logs
    environment:
      - LOG_LEVEL=INFO
