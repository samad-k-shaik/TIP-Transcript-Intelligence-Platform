version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./datasets:/app/datasets
      - ./parquet:/app/parquet
      - ./logs:/app/logs
    environment:
      - LOG_LEVEL=INFO
